export class Contract {
  constructor(_opts: { certificateWitness: (context: any) => any }) {
    // Stub: real Contract is compiled from contracts/midnight-certs/src/certificate_proof.compact
    // Run: cd contracts/midnight-certs && bun run build
  }
}
